<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <title><%= title %></title>
  <%= stylesheet_link_tag :application, :media => 'all' %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <%= render 'flashes' -%>
  <div class="container-narrow">
    <div class="masthead">
      <%= render 'layouts/user_header' %>
      <h3 class="muted"><%= link_to "99 Problems", root_path %></h3>
      <h5 class="muted">I got 99 problems, son, but listing them in chronological order ain't one.</h5>
    </div>
    <hr />
    <%= yield %>
  </div>
  <%= render 'javascript' %>
</body>
</html>
